{
  "name": "stockroom-demo",
  "scripts": {
    "start": "preact watch",
    "build": "preact build",
    "serve": "preact serve",
    "deploy": "surge -d https://stockroom.surge.sh build"
  },
  "devDependencies": {
    "preact-cli": "^2.1.1",
    "worker-loader": "^1.1.0"
  },
  "dependencies": {
    "preact": "^8.2.7",
    "preact-material-components": "^1.3.4",
    "unistore": "^3.0.3"
  }
}
